function sStatus = wDeleteProduct(Wasdi, sProductName)%Deletes a product in active workspace%Syntax%sStatus = wDeleteProduct(Wasdi, sProductName)%%:param Wasdi: Wasdi object created after the wasdilib call%:param sFileName: Name of the file to add%%:Returns:%   :sStatus: empty string if deletion was successful, null in case it did not work  if exist("Wasdi") < 1     disp('Wasdi variable does not exist')    return   end      sStatus = char(Wasdi.deleteProduct(sProductName));end