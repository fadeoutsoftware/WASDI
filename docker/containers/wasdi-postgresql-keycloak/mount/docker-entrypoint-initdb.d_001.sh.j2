#!/bin/bash

set -e

psql -v ON_ERROR_STOP=1 --username "${POSTGRES_USER}" --dbname "${POSTGRES_DB}" <<-EOSQL
    CREATE ROLE ${POSTGRES_USER1_NAME} WITH SUPERUSER NOCREATEROLE NOCREATEDB NOINHERIT LOGIN NOREPLICATION NOBYPASSRLS PASSWORD '${POSTGRES_USER1_PASSWORD}';
EOSQL
