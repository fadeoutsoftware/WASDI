# Listen on 0.0.0.0:{{ sWasdiRabbitMqMainInternalPort }}
#listeners.tcp.default = 0.0.0.0:{{ sWasdiRabbitMqMainInternalPort }}
listeners.tcp.default = {{ sWasdiRabbitMqMainInternalPort }}

# Load definition when node boot
load_definitions = /etc/rabbitmq/definitions.json

# Enable log in console
log.console = true
log.console.level = debug

# Disable log in files
log.file = false

# Disable log through syslog
log.syslog = false

# Force the web stomp port on {{ sWasdiRabbitMqWebStompInternalPort }}
web_stomp.tcp.port = {{ sWasdiRabbitMqWebStompInternalPort }}
